# Report — สรุปปัญหาจาก "สิ่งที่อยากสอบถามรุ่นพี่รอบที่ 2"

**วันที่:** 01/09/2026
**แหล่งอ้างอิง:** `สิ่งที่อยากสอบถามรุ่นพี่รอบที่ 2.pdf`
**สถานะ:** ตรวจสอบโค้ดแล้ว ✅ และแก้ไขปัญหาที่ 2 แล้ว ✅

---

## สรุปสั้น (TL;DR)

| # | ประเด็นที่ถามใน PDF | ผลการวิเคราะห์ | สถานะ |
|---|---|---|---|
| 1 | บัญชี `supervised` กับ `admin` ต่างกันไหม / ล็อกอิน admin ไม่ได้ / ทำไมอีเมลเป็น `go.th` | **ไม่ใช่บั๊ก** — เป็นการออกแบบระบบสิทธิ์ (Role) + เงื่อนไขความปลอดภัยตอน seed | อธิบายด้านล่าง |
| 2 | กด "บันทึกตอบกลับ" ในช่องทางติดต่อแล้วสถานะไม่หาย ไม่มีแจ้งเตือน | **เป็นบั๊กจริง** — frontend ยิงไปผิด path/method (`PATCH /api/contact/:id` ที่ไม่มีอยู่) แล้วเก็บ error แบบเงียบ | **แก้แล้ว** |

---

## ประเด็นที่ 1: บัญชี Supervisor กับ Admin

### คำถามเดิมใน PDF
> "อีเมลกับรหัสมันมีของตัว supervised กับ admin ใช่ไหมครับ? ตัวของ admin มีหน้าที่ทำอะไรหรอครับ เอาไว้ทดสอบตัวอะไรรึป่าวครับ พอดีผมไม่สามารถล็อกอินเข้าไปดูได้ หรือต้องไปสร้างตัว admin ใน supervised เองหรือป่าวครับ และผมเห็นว่าพี่กำหนดให้เป็น go.th ด้วย"

### คำตอบจากการตรวจสอบโค้ด

ระบบมี 2 role ในส่วนแดชบอร์ด (`backend/prisma/seed.ts`, `backend/middlewares/auth.ts`):

| Role | บทบาท | สิทธิ์หลัก |
|---|---|---|
| **SUPERVISOR** | ผู้ดูแลสูงสุด (root) | สร้าง/แบน/unban admin, ดู Audit Log (`restrictTo('SUPERVISOR')`), อนุมัติคำขอ, Force logout / Emergency recovery |
| **ADMIN** | เจ้าหน้าที่ปฏิบัติงาน | จัดการเนื้อหาเว็บ: ข่าว, ประชาสัมพันธ์, แผนก, คอมเมนต์, **ข้อความติดต่อ (Contact)** |

- **Admin ไม่ใช่แค่ "ตัวทดสอบ"** — เป็น role ปฏิบัติงานจริงของเจ้าหน้าที่ทั่วไป ส่วนบัญชี admin ที่ seed สร้างไว้เป็นเพียง **บัญชี default ไว้เริ่มใช้งานระบบ**
- **อีเมล/รหัสของทั้งสองบัญชีมาจากไฟล์ `.env`** ไม่ได้ hardcode ในโค้ด (ชื่อตัวแปร: `SEED_SUPERVISOR_EMAIL`, `SEED_SUPERVISOR_PASSWORD`, `SEED_ADMIN_EMAIL`, `SEED_ADMIN_PASSWORD` — ดูได้จาก `.env.example`) จึงต้องดูรหัสจาก `.env` ของเครื่อง/เซิร์ฟเวอร์ที่รัน seed เท่านั้น

### ทำไมล็อกอิน admin เข้าไปดูไม่ได้?

พบ 2 เงื่อนไขที่ทำให้ "ล็อกอินไม่ได้" ตามอาการที่เล่า:

1. **`seed.ts` ตั้ง `forcePasswordReset: true` ให้บัญชี admin เสมอ** (บรรทัด 113) — เข้าสู่ระบบครั้งแรกจะถูก redirect ไปหน้า `/force-password-reset` ทันที (`frontend/src/routes/ProtectedRoute.tsx` บรรทัด 27–30) เพื่อบังคับเปลี่ยนรหัสผ่าน โดยต้องยืนยัน **OTP ทางอีเมล**
2. **SMTP ที่ตั้งไว้ตอนนี้เป็น `smtp.ethereal.email`** (mail server จำลองสำหรับ dev) → OTP ไม่ได้ถูกส่งเข้า inbox จริง ต้องไปดูที่ inbox ของ Ethereal เท่านั้น ทำให้ดูเหมือน "ระบบเงียบ / ล็อกอินต่อไม่ได้"

> เทียบกับบัญชี supervisor ที่ seed ตั้ง `forcePasswordReset: false` จึงเข้าใช้ได้ทันทีด้วยรหัสใน `.env`

### เรื่องอีเมล `.go.th`

ไม่ใช่ความผิดพลาด — ระบบบังคับโดเมนองค์กรรัฐตามธีมเว็บ AMLO (จำลอง ปปง.) อยู่ 2 จุด:
- `backend/controllers/authController.ts` บรรทัด 58–65 → สมัคร/ใช้งานต้องลงท้าย `.go.th` ไม่งั้นตอบ `400: Only organization email (.go.th) is allowed`
- `backend/controllers/adminUserController.ts` บรรทัด 87 → ตอน Supervisor สร้าง admin ใหม่ก็บังคับเช่นกัน

### ข้อเสนอแนะ (วิธีใช้งานที่ถูกต้อง)

- ✅ ดู email/password ของ admin จาก `.env` (`SEED_ADMIN_*`)
- ✅ เมื่อล็อกอินแล้วระบบจะพาไปตั้งรหัสใหม่ผ่าน OTP — ถ้า dev อยู่บน Ethereal ให้เปิด inbox ที่ ethereal.email ด้วย account ใน `.env` (`SMTP_USER`) เพื่อรับ OTP
- ✅ หรือวิธีที่แนะนำกว่า: **ให้ Supervisor สร้างบัญชี admin ใหม่เองจากหน้าจัดการผู้ใช้ (CreateAdminModal)** แล้วค่อยแบน/ลบบัญชี seed ทิ้ง — ตอบคำถามเดิมว่า "ต้องไปสร้างตัว admin ใน supervised เองหรือป่าว" → **ใช่ ทางนั้นถูกต้องแล้ว**

---

## ประเด็นที่ 2: กด "บันทึกตอบกลับ" แล้วไม่หาย ไม่มีแจ้งเตือน

### คำถามเดิมใน PDF
> "ตรงช่องทางติดต่อ ผมได้ทดสอบแล้ว มันใช้งานได้ และกำลังจะกดบันทึกตอบกลับแล้ว แต่พอบันทึกก็ไม่หายครับ ไม่ขึ้นแจ้งเตือนอะไรเลยด้วยครับ ... ปล่อยไว้ได้เลยมั้ยครับ หรือต้องลบตรงไหนครับ ไม่แน่ใจว่าเจอเรื่อง path อะไรไหมครับ"

### สาเหตุ (ยืนยันจากโค้ด) — เดาถูกแล้วครับ เจอเรื่อง path จริง 🔴

**Frontend** (`frontend/src/components/dashboard/ContactRequestManager.tsx` — โค้ดก่อนแก้):

```ts
const res = await api<{ success: boolean }>(`/contact/${id}`, {
  method: 'PATCH',                              // ❌ ผิด method
  body: JSON.stringify({ status: newStatus }),  // ❌ stringify ซ้ำ + ไม่มี id
})
...
} catch {
  /* silent */                                  // ❌ เก็บ error แบบเงียบ
}
```

**Backend** (`backend/routes/contactRoute.ts`) ไม่มี route นี้อยู่จริง:

| Route ที่ backend มี | ที่ frontend ยิงไป |
|---|---|
| `POST /api/contact` (public) | — |
| `GET /api/contact` | ✅ ใช้ถูก |
| `PUT /api/contact/update` ← ตัวจริงที่ใช้เปลี่ยนสถานะ | ❌ `PATCH /api/contact/:id` → **404** |

ผลลัพธ์: fetch ได้ **404** → `api.ts` throw error → `catch { /* silent */ }` กลืนทิ้ง → **สถานะไม่เปลี่ยน ไม่มี toast ไม่มีอะไรเกิดขึ้นเลย** = อาการ "พอบันทึกก็ไม่หาย ไม่ขึ้นแจ้งเตือน" พอดี

บั๊กแฝงเพิ่มเติม: `api()` มี `JSON.stringify(body)` อยู่ในตัวแล้ว โค้ดเดิม stringify ซ้ำเป็นชั้นที่สอง และ backend ต้องการ `{ id, status }` ไม่ใช่แค่ `{ status }`

### สิ่งที่แก้ไปแล้ว ✅

แก้ที่ `frontend/src/components/dashboard/ContactRequestManager.tsx`:

- ลบฟังก์ชัน `handleReplyToggle` (โค้ดยิง API เองแบบผิด path) ทิ้ง
- เปลี่ยนปุ่ม "บันทึก \"ตอบกลับแล้ว\"" ให้เรียกใช้ store ที่ถูกต้องอยู่แล้ว: `useDashboardStore → contacts.updateStatus(id, status)` ซึ่งไปทาง `dashboardService.updateContactStatus` → **`PUT /api/contact/update`** พร้อมส่ง `{ id, status }` ให้ครบ

ผลที่ได้หลังแก้ (ครบทุกอย่างที่โค้ดเดิมขาด):
- ✅ ยิงถูก endpoint สถานะเปลี่ยนจริงในฐานข้อมูล
- ✅ มี dialog ยืนยันก่อนบันทึก + loading
- ✅ **มี toast แจ้งเตือน "อัปเดตสำเร็จ" หรือแจ้ง error กรณีพลาด** (ไม่เงียบอีกต่อไป)
- ✅ โหลดรายการใหม่ให้อัตโนมัติหลังบันทึก
- ✅ ตรวจสอบด้วย `tsc --noEmit` แล้ว **ไม่มี TypeScript error**

### ตอบคำถาม "ต้องปล่อยไว้ / ลบตรงไหนไหม?"

- **ไม่ต้องลบข้อมูลอะไรทั้งสิ้น** — ปัญหาอยู่ที่โค้ดฝั่ง frontend จุดเดียว ข้อมูลในฐานข้อมูล (`contact_requests`) ปลอดภัยและใช้ได้ปกติ
- แก้โค้ดแล้ว กดบันทึกซ้ำกับรายการเดิมได้เลย สถานะจะเปลี่ยนและขึ้นแจ้งเตือนตามปกติ

---

## สิ่งที่ควรทำตาม (Action Items)

1. **Pull โค้ดรอบนี้** เพื่อรับการแก้ ContactRequestManager แล้วทดสอบ: แดชบอร์ด → Contact → กด "บันทึก \"ตอบกลับแล้ว\"" → ควรขึ้น toast "อัปเดตสำเร็จ" และสถานะเปลี่ยน
2. **เรื่อง SMTP/OTP:** ถ้าจะให้ admin ตั้งรหัสผ่านใหม่ได้จริง ต้องตั้ง `SMTP_*` เป็น mail server จริง (หรือดู OTP จาก Ethereal inbox ชั่วคราวตอน dev)
3. **บริหารบัญชี:** ให้ Supervisor สร้าง admin จริงจากหน้า User Management แทนการใช้บัญชี seed default และเก็บรหัส `SEED_*` ไว้เป็นความลับ (ห้าม commit `.env`)

---
*ตรวจสอบโดย Cline — วิเคราะห์จาก PDF + โค้ดจริงใน repo (backend/prisma/seed.ts, backend/routes/contactRoute.ts, backend/controllers/contactController.ts, frontend/src/components/dashboard/ContactRequestManager.tsx, frontend/src/stores/useDashboardStore.ts, frontend/src/utils/api.ts)*

